import React from 'react';

const Page = () => {
    return (
        <div>
            Do not worry about this, the chat will be here as it has already been handled in main.
        </div>
    );
}

export default Page;
